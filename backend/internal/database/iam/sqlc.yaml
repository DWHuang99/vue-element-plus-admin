version: "2"

sql:
  - engine: "postgresql"
    schema: "../../../migrations/iam"
    queries: "query.sql"
    gen:
      go:
        package: "iamdb"
        out: "generated"
        emit_json_tags: true
