version: "2"

sql:
  - engine: "postgresql"
    schema: "../../../migrations/department"
    queries: "query.sql"
    gen:
      go:
        package: "departmentdb"
        out: "generated"
        emit_json_tags: true
