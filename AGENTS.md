# 项目协作规范

## 校验结果分类

每次进行代码校验、代码审查、测试或运行检查时，必须将发现的问题分为以下两类分别报告，不得混在一起：

### 业务问题

指代码可以编译或运行，但实际业务行为、接口契约或用户体验不符合预期的问题，例如：

- HTTP 状态码或业务错误码不正确。
- 返回文字、字段名称或数据结构与约定不匹配。
- 表单校验、权限判断、登录注册流程等业务逻辑错误。
- 前后端接口参数或响应约定不一致。

### 工程问题

指影响项目构建、运行、部署、可靠性或数据安全的问题，例如：

- 编译、类型检查、测试或构建失败。
- 容器、依赖、配置或数据库连接无法正常工作。
- 数据未持久化、迁移未执行或生成代码未更新。
- 资源泄漏、启动失败或运行时崩溃。

校验结果中必须分别给出“业务问题”和“工程问题”两个部分。某一类没有发现问题时，也要明确写“无”。
